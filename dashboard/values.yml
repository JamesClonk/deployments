#@data/values
---
app: ${KUBE_APP_NAME}
namespace: kubernetes-dashboard

domains:
- ${KUBE_DOMAIN_ALPHA}
- ${KUBE_DOMAIN_BETA}

dashboard:
  auth: "${DASHBOARD_BASIC_AUTH}"
  username: "${DASHBOARD_BASIC_AUTH_USERNAME}"
  password: "${DASHBOARD_BASIC_AUTH_PASSWORD}"
