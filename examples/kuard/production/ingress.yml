apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: kuard
spec:
  rules:
  - host: kuard.51.15.242.160.xip.io
    http:
      paths:
      - backend:
          serviceName: kuard
          servicePort: 80
