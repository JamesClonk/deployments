apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: cf-env
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: cf-env.${KUBE_DOMAIN}
    http:
      paths:
      - backend:
          serviceName: cf-env
          servicePort: 80
