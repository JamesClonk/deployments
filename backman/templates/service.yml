apiVersion: v1
kind: Service
metadata:
  name: backman
  namespace: backman
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: backman
    app.service: backman
    app.service.component: gui
