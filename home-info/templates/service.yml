apiVersion: v1
kind: Service
metadata:
  name: home-info-dashboard
  namespace: home-info
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: home-info
    app.service.component: dashboard
